import buttonText from 'assets/constants/button';
import CustomButtonSidebar from 'components/button/button-custom';

const SideBarButton = () => {
  return <CustomButtonSidebar id={''}>{buttonText.save}</CustomButtonSidebar>;
};

export default SideBarButton;
