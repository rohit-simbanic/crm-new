import { ObjectType } from 'types';

const buyerSource: ObjectType = {
  entera_client: 'Entera Client',
  entera_known: 'Entera Known',
  direct_unknown: 'Direct (Unknown)'
};

export default buyerSource;
