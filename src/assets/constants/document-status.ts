import { ObjectType } from 'types';

const documentStatus: ObjectType = {
  published: 'Published',
  private: 'Private',
  active: 'Active',
  deactive: 'Deactive'
};

export default documentStatus;

export const documentStatus2: ObjectType = {
  published: 'Published',
  private: 'Private',
  replace: 'Replace'
};
