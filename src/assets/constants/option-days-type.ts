import { ObjectType } from 'types';

const optionDaysType: ObjectType = {
  business: 'Business',
  calendar: 'Calendar',
  calendar_days_no_weekend_closings: 'Calendar No Weekend Closings'
};

export default optionDaysType;
