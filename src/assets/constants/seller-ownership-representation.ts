import { ObjectType } from 'types';

const sellerOwnershipRepresentation: ObjectType = {
  at_least_one: 'At least 1 year',
  less_than_one: 'Less than 1 year'
};

export default sellerOwnershipRepresentation;
