import { ObjectType } from 'types';

const accessType: ObjectType = {
  lockbox: 'Lockbox',
  supra: 'Supra',
  seller_agent_will_provide_access: 'Seller/agent will provide access',
  other: 'Other'
};

export default accessType;
