import { ObjectType } from 'types';

const wireStatus: ObjectType = {
  awaiting_approval: 'Awaiting Approval',
  requested: 'Requested',
  entered: 'Entered',
  denied: 'Denied',
  blank: 'Blank'
};

export default wireStatus;
