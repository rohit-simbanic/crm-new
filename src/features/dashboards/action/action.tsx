import ActionDropdown from 'components/action/dropdown';

const HoverableAction = ({ data }: any) => {
  return (
    <>
      <ActionDropdown data={data} />
    </>
  );
};

export default HoverableAction;
